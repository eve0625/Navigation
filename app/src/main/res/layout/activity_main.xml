<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".main.MainActivity">

    <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/main_bottom_navigation_height"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/nav_view"
            android:layout_width="0dp"
            android:layout_height="@dimen/main_bottom_navigation_height"
            android:layout_marginEnd="0dp"
            android:layout_marginStart="0dp"
            android:background="?android:attr/windowBackground"
            android:foreground="@null"
            app:itemBackground="@color/white"
            app:itemIconTint="@drawable/selector_color_bottom_tab"
            app:itemTextColor="@drawable/selector_color_bottom_tab"
            app:itemIconSize="24dp"
            app:labelVisibilityMode="labeled"
            app:itemTextAppearanceActive="@style/MainNavigationItemText"
            app:itemTextAppearanceInactive="@style/MainNavigationItemText"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:menu="@menu/main_nav_menu"/>

</androidx.constraintlayout.widget.ConstraintLayout>